# Medical Trials Portal (Prototype)

Minimal Vite + React prototype so you can run the UI locally.

Quick start

```bash
npm install
npm run dev
# open http://localhost:5173 in your browser
```

Files of interest:
- `index.html` — app entry
- `src/main.jsx` — React bootstrap
- `src/App.jsx` — main UI
